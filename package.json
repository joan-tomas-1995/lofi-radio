{
  "name": "lofi-radio-monorepo",
  "private": true,
  "workspaces": [
    "lofi-radio"
  ],
  "scripts": {
    "build": "npm --prefix lofi-radio run build",
    "lint": "npm --prefix lofi-radio run lint",
    "deploy": "cd lofi-radio && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
